@model IEnumerable<MVC.Views.Participation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fonction)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Employe.NomE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Projet.NomP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fonction)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Employe.NomE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Projet.NomP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
