@model IEnumerable<MVC.Views.Projet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodeP }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodeP }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodeP })
        </td>
    </tr>
}

</table>
